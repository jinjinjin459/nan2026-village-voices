# 2026-08-10 작업 기록

## 목표

NAN 2026 사전 과제용 `마을의 목소리`를 영상 제외 바로 제출 가능한 상태로 완성한다.

## 완료 증거

- `npm test`: 4/4 PASS
- `npm run build`: PASS
- fallback E2E: `intro→lulu→moka→park→moka→lulu→result` PASS
- Gemini 3.6 Flash REST: HTTP 200, structured dialogue 검증 PASS
- live AI UI: source badge `AI` PASS
- secret pattern scan: PASS
- 공개 source repo: `jinjinjin459/nan2026-village-voices`

## 중요한 구현 판단

- AI는 대사·감정·주제만 표현하고 상태 변화 권한이 없다.
- 브라우저가 보낸 관계·사건 문자열은 신뢰하지 않고 서버가 phase/selectedFacility로 scenario truth를 재구성한다.
- 공개 정적 Pages는 키를 포함하지 않으며 API 실패 시 상태별 fallback으로 완주한다.
- 로컬/서버 런타임만 `GEMINI_API_KEY`를 읽는다.
- Gemini 3.6 REST v1beta는 실제 호출에서 structured MIME enum `APPLICATION_JSON`이 필요했다.
- 낮은 `maxOutputTokens`가 thinking 출력까지 잘라 제거했다.

## 다음 단계

Pages workflow와 공개 URL을 확인하고, HTML 문서 원본을 Edge headless로 PDF 변환한 뒤 Poppler로 페이지·텍스트·폰트·시각 렌더를 검증한다.
